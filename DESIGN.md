技术设计文档 —— 天大二手书智能匹配助手


一、项目概述

项目名称：agent2026-book-match
作品定位：基于大语言模型的校园二手书智能匹配工具，帮助天津大学在校生快速找到想买的教材，或将闲置教材转售给需要的同学。
目标用户：天津大学全体在校本科生、研究生。
核心价值：节约购书开支、提高匹配效率、促进书籍循环利用。


二、整体架构

本项目分为四个层级：
1.用户交互层：包含 app.py（Streamlit图形界面，提供网页端操作入口）和 main.py（命令行界面，支持 add、start、test 等命令）。
2.业务逻辑层：包含 matcher.py（从数据库读取未匹配需求，调用AI进行语义匹配）、notifier.py（为买卖双方分别生成个性化通知文案）和 scheduler.py（定时触发匹配任务，默认每5分钟）。
3.AI能力层：包含 llm_client.py（封装校内词元服务API，提供统一调用接口）。
4.数据存储层：包含 db.py（SQLite数据库的增删改查操作）。
此外，config.py 作为配置层，集中管理API地址、密钥、通知渠道等配置。


三、核心功能流程

用户提交需求（买或卖，填写书名和联系方式）→ 数据存入SQLite数据库（demands表，matched状态为0）→ 立即触发快速匹配（或等待定时调度每5分钟执行）→ 读取所有未匹配需求，分离买方列表和卖方列表 → 遍历每对买/卖组合，调用 is_same_book 函数（使用校内大模型判断书名是否一致）→ 若匹配成功：将双方需求标记为已匹配、记录匹配对、生成并发送通知 → 买家收到包含卖家联系方式的通知，卖家收到包含买家联系方式的通知。若匹配失败，等待下次定时扫描。


四、校内词元服务调用（大赛强制要求）

API地址：https://ai.tju.edu.cn/api/agent2026/gitlab-49-agent2026-gl-bot/chat/completions
认证方式：Bearer Token（API Key）
模型名称：tju-llm
速率限制：60次/分钟，3000次/小时
调用额度：赛事全周期不限额

核心调用函数 call_tju_llm：接收用户输入的提示词和温度参数，向校内词元服务发送请求，返回模型生成的文本内容。
书名语义匹配函数 is_same_book：接收两个书名，构造提示词调用 call_tju_llm，根据返回结果判断是否为同一本书。
通知生成函数 notify_match：接收匹配记录（包含书名、买家联系方式、卖家联系方式），分别生成买家版和卖家版通知文案。买家版只显示卖家联系方式，卖家版只显示买家联系方式。


五、数据模型设计

demands表字段：
id：整数类型，主键自增
type：文本类型，buy表示求购，sell表示出售
book_name：文本类型，用户输入的书名
contact：文本类型，联系方式（微信/电话/邮箱）
matched：整数类型，0表示未匹配，1表示已匹配
created_at：时间戳类型，提交时间，默认为当前时间

主要数据库操作函数：
init_db：初始化数据库，创建demands表
add_demand：添加一条买或卖需求，返回需求ID
get_unmatched_demands：获取所有未匹配的需求列表
mark_as_matched：将指定需求标记为已匹配


六、技术栈选型

开发语言：Python 3.9以上，简单易学，生态丰富
Web界面：Streamlit，纯Python构建，无需前端知识
数据库：SQLite，轻量级无需额外部署
定时调度：schedule库，轻量级无需额外服务
HTTP请求：requests库，稳定可靠
版本控制：Git，满足大赛多次提交要求
AI服务：校内词元服务，大赛强制要求且不限额


七、部署与运行

本地运行方式：
安装依赖：pip install -r requirements.txt
启动图形界面（校园网内可被其他设备访问）：streamlit run src/app.py --server.address 0.0.0.0
访问地址为 http://本机IP:8501

命令行运行方式：
添加需求：python src/main.py add buy "书名" "联系方式"
添加卖方需求（会触发快速匹配）：python src/main.py add sell "书名" "联系方式"
启动定时调度（每5分钟自动匹配）：python src/main.py start

云端部署：已部署至Streamlit Cloud，公网链接可访问前端界面。完整AI匹配功能需在校园网或VPN环境下使用（校内词元服务仅限校园网访问）。


八、已知限制与未来扩展

已知限制：AI匹配依赖校内词元服务，仅限校园网或VPN环境访问，云端部署时无法完成匹配。数据存储在本地SQLite文件中。联系方式仅支持自由文本输入，未做格式校验。
未来扩展方向：接入企业微信或邮件真实发送能力（当前为控制台模拟）。增加用户认证系统防止恶意提交。支持图片识别（拍照识别书名）。增加匹配历史记录与统计看板。支持多校区独立匹配。